# Environment Variables

# Next.js
# Note: NextAuth is no longer used for authentication (kept for backward compatibility)
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google OAuth (Client-side)
# Note: Only NEXT_PUBLIC_GOOGLE_CLIENT_ID is needed for client-side OAuth
# Google login is now handled directly by the browser calling backend /api/v1/auth/google-login
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id

# Legacy Google OAuth (for NextAuth - can be removed if NextAuth is completely removed)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
