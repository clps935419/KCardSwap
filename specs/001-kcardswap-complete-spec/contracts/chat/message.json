{
  "id": "chat.message.send",
  "title": "發送聊天訊息",
  "endpoint": "/api/chat/messages",
  "method": "POST",
  "request": {
    "type": "object",
    "properties": {
      "from_user": { "type": "string" },
      "to_user": { "type": "string" },
      "content": { "type": "string" }
    },
    "required": ["from_user", "to_user", "content"]
  },
  "response": {
    "type": "object",
    "properties": {
      "message_id": { "type": "string" },
      "sent_at": { "type": "string", "format": "date-time" }
    },
    "required": ["message_id", "sent_at"]
  },
  "implemented": false,
  "notes": "Chat send message contract."
}
