name: Schema alignment tests (disabled)

on: {}

jobs:
  schema-alignment-tests:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "schema alignment tests workflow disabled"
